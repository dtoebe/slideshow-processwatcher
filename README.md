# slideshow-processwatcher

Watches the processes of the slideshow defined in settings.ini file then sends it to the webserver

### TODO
* if socket server (slideshow-webserver) is not listening retry after n time
* Clean Up logging
